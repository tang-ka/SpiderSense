using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//jet에 타고 있을 때와 내렸을때를 구분
public class SB_GreenGoblin : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
